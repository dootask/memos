services:
  memos-server:
    image: dootask/memos-server:0.29.0
    restart: always
    volumes:
      - ./data/memos:/var/opt/memos
    environment:
      - MEMOS_MODE=prod
      - MEMOS_DRIVER=sqlite

  memos-proxy:
    image: dootask/memos:${PLUGIN_VERSION}
    restart: always
    depends_on:
      - memos-server
    environment:
      - PROXY_PORT=7070
      - MEMOS_UPSTREAM=http://memos-server:5230
      - DOOTASK_URL=http://nginx
      - PUBLIC_BASE=/apps/memos
      - MEMOS_ADMIN_USER_IDS=${MEMOS_ADMIN_USER_IDS}
      - INTERNAL_SECRET=${INTERNAL_SECRET}
